# datasets/processed/

Cleaned, labeled, and feature-engineered datasets ready for AI model training and evaluation.

Files in this folder are derived from `../raw/` after:

- Removing duplicates and invalid entries
- Standardizing field formats
- Adding labels (bug category, severity)
- Splitting into train/validation/test sets
