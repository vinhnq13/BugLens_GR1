# datasets/raw/

Raw, unprocessed bug report data.

Store original CSV, JSON, or XML files here before any cleaning or transformation.

**Do not** modify files in this folder directly — copy them to `../processed/` after cleaning.
